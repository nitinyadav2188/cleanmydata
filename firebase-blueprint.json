{
  "entities": {
    "ToolStat": {
      "title": "ToolStat",
      "description": "Tracks the usage count of a specific tool.",
      "type": "object",
      "properties": {
        "count": {
          "type": "number",
          "description": "The number of times this tool has been used."
        }
      },
      "required": ["count"]
    }
  },
  "firestore": {
    "/tool_stats/{toolId}": {
      "schema": {
        "$ref": "#/entities/ToolStat"
      },
      "description": "Stores usage statistics for each tool."
    }
  }
}
